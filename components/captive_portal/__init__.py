# Passthrough — lets ESPHome's built-in captive_portal load normally.
# This file exists only so the external_components declaration can include
# captive_portal without ESPHome complaining about a missing module.
# All actual work is done by the built-in captive_portal component.
